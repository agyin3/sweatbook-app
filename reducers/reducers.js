export const reducer = (state = initialState, action) => {
    switch (action.payload) {
        case value:
            return state
            break;
    
        default:
            return state
            break;
    }
}

const initialState = {

}